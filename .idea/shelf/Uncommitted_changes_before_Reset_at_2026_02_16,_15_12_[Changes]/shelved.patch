Index: backend/blackjack_engine.py
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.BaseRevisionTextPatchEP
<+>import random\nfrom typing import Any, Literal\n\n\nclass GameEngine:\n    def __init__(self) -> None:\n        self.suits = [\"hearts\", \"diamonds\", \"spades\", \"clubs\"]\n        self.user_cards = []\n        self.computer_card = []\n        self.user_points = 0\n        self.computer_points = 0\n        self.computer_hidden_point = 0\n        # Tracks whether we've already adjusted an ace from 11->1 for the current user turn\n        self.user_ace_adjusted = False\n        self.computer_ace_adjusted = False\n        self.heart_ranks = [\n            \"ace\",\n            \"two\",\n            \"three\",\n            \"four\",\n            \"five\",\n            \"six\",\n            \"seven\",\n            \"eight\",\n            \"nine\",\n            \"ten\",\n            \"jack\",\n            \"queen\",\n            \"king\",\n        ]\n        self.diamond_ranks = [\n            \"ace\",\n            \"two\",\n            \"three\",\n            \"four\",\n            \"five\",\n            \"six\",\n            \"seven\",\n            \"eight\",\n            \"nine\",\n            \"ten\",\n            \"jack\",\n            \"queen\",\n            \"king\",\n        ]\n        self.spade_ranks = [\n            \"ace\",\n            \"two\",\n            \"three\",\n            \"four\",\n            \"five\",\n            \"six\",\n            \"seven\",\n            \"eight\",\n            \"nine\",\n            \"ten\",\n            \"jack\",\n            \"queen\",\n            \"king\",\n        ]\n        self.club_ranks = [\n            \"ace\",\n            \"two\",\n            \"three\",\n            \"four\",\n            \"five\",\n            \"six\",\n            \"seven\",\n            \"eight\",\n            \"nine\",\n            \"ten\",\n            \"jack\",\n            \"queen\",\n            \"king\",\n        ]\n        self.budget = 1000\n        self.points_dictionary = {\n            \"ace\": 1,\n            \"two\": 2,\n            \"three\": 3,\n            \"four\": 4,\n            \"five\": 5,\n            \"six\": 6,\n            \"seven\": 7,\n            \"eight\": 8,\n            \"nine\": 9,\n            \"ten\": 10,\n            \"jack\": 10,\n            \"queen\": 10,\n            \"king\": 10,\n        }\n\n    def new_game_state(self) -> tuple[list, int, Literal['BLACKJACK'], int] | tuple[list, int, None, None]:\n        \"\"\"This method is called at the start of a new game to deal the initial two cards for the user and compute the initial points.\n        It also checks for an initial blackjack and returns the appropriate result if so.\"\"\"\n\n        card_suit = \"\"\n        rank = \"\"\n\n        for card in range(2):\n            while True:\n                card_suit = random.choice(self.suits)\n\n                if card_suit == \"hearts\" and len(self.heart_ranks) > 0:\n                    rank = random.choice(self.heart_ranks)\n                    self.heart_ranks.remove(rank)\n                    break\n\n                elif card_suit == \"diamonds\" and len(self.diamond_ranks) > 0:\n                    rank = random.choice(self.diamond_ranks)\n                    self.diamond_ranks.remove(rank)\n                    break\n\n                elif card_suit == \"spades\" and len(self.spade_ranks) > 0:\n                    rank = random.choice(self.spade_ranks)\n                    self.spade_ranks.remove(rank)\n                    break\n\n                elif card_suit == \"clubs\" and len(self.club_ranks) > 0:\n                    rank = random.choice(self.club_ranks)\n                    self.club_ranks.remove(rank)\n                    break\n\n            self.user_cards.append((card_suit, rank))\n\n        # start of a new hand / user turn -> reset ace-adjust flag\n        self.user_ace_adjusted = False\n\n        initial_user_cards = self.user_cards\n        user_card_value1 = initial_user_cards[0][1]\n        user_card_value2 = initial_user_cards[1][1]\n\n        for user_point1 in self.points_dictionary:\n            if user_card_value1 in self.points_dictionary:\n                if user_card_value1 == \"ace\":\n                    self.user_points += 10\n                    self.user_points += self.points_dictionary[user_card_value1]\n                    break\n                else:\n                    self.user_points += self.points_dictionary[user_card_value1]\n                    break\n\n        for user_point2 in self.points_dictionary:\n            if user_card_value2 in self.points_dictionary:\n                if user_card_value1 == \"ace\" and user_card_value2 in (\"ten\", \"jack\", \"queen\", \"king\"):\n                    self.user_points += self.points_dictionary[user_card_value2]\n                    self.computer_points += self.computer_hidden_point\n                    return self.user_cards, self.user_points, \"BLACKJACK\", self.computer_points\n                elif self.user_points == 10 and user_card_value2 == \"ace\":\n                    self.user_points += 10\n                    self.user_points += self.points_dictionary[user_card_value2]\n                    self.computer_points += self.computer_hidden_point\n                    return self.user_cards, self.user_points, \"BLACKJACK\", self.computer_points\n                elif user_card_value2 == \"ace\" and user_card_value1 !=\"ace\":\n                    self.user_points += 10\n                    self.user_points += self.points_dictionary[user_card_value2]\n                    return self.user_cards, self.user_points, None, None\n                else:\n                    self.user_points += self.points_dictionary[user_card_value2]\n                    break\n\n        return self.user_cards, self.user_points, None, None\n\n    def _hand_total(self, cards: list[tuple]) -> int:\n        \"\"\"Return best total for a hand treating aces as 1 or 11.\n\n        Uses base values from `self.points_dictionary` (ace=1) and upgrades\n        as many aces from 1->11 as possible without busting.\n        \"\"\"\n        total = 0\n        aces = 0\n        for suit, rank in cards:\n            if rank == \"ace\":\n                aces += 1\n                total += self.points_dictionary[rank]\n            else:\n                total += self.points_dictionary[rank]\n\n        # Upgrade some aces from 1 -> 11 (i.e. add 10) while it doesn't bust\n        for _ in range(aces):\n            if total + 10 <= 21:\n                total += 10\n            else:\n                break\n\n        return total\n\n    def ai_new_game(self) -> tuple[list, int, Literal['BLACKJACK'], Literal['PUSH']] | tuple[list, int, Literal['BLACKJACK'], None]:\n        \n        \"\"\"This method is called immediately after new_game_state() to set up the computer's hand and points based on the initial two cards. \n        It also checks for an initial blackjack and returns the appropriate result if so.\"\"\"\n        \n        card_suit = \"\"\n        rank = \"\"\n\n        # Deal two initial cards for the computer\n        for card in range(2):\n            while True:\n                card_suit = random.choice(self.suits)\n\n                if card_suit == \"hearts\" and len(self.heart_ranks) > 0:\n                    rank = random.choice(self.heart_ranks)\n                    self.heart_ranks.remove(rank)\n                    break\n\n                elif card_suit == \"diamonds\" and len(self.diamond_ranks) > 0:\n                    rank = random.choice(self.diamond_ranks)\n                    self.diamond_ranks.remove(rank)\n                    break\n\n                elif card_suit == \"spades\" and len(self.spade_ranks) > 0:\n                    rank = random.choice(self.spade_ranks)\n                    self.spade_ranks.remove(rank)\n                    break\n\n                elif card_suit == \"clubs\" and len(self.club_ranks) > 0:\n                    rank = random.choice(self.club_ranks)\n                    self.club_ranks.remove(rank)\n                    break\n\n            self.computer_card.append((card_suit, rank))\n\n        # Use the robust hand-total helper to compute points\n        total = self._hand_total(self.computer_card)\n        self.computer_points = total\n        # After computing total we don't need a separate hidden-point mechanic\n        self.computer_hidden_point = 0\n\n        # Check for initial blackjack\n        if total == 21:\n            if total == self.user_points:\n                return self.computer_card, total, \"BLACKJACK\", 'PUSH'\n            else:\n                return self.computer_card, total, \"BLACKJACK\", None\n\n        return self.computer_card, total, None, None\n\n    def check_deck(self) -> dict[str, int]:\n        card_count = (\n            len(self.heart_ranks)\n            + len(self.diamond_ranks)\n            + len(self.spade_ranks)\n            + len(self.club_ranks)\n        )\n        return {\"card_count\": card_count}\n\n    def reset_game(self) -> None:\n        self.user_cards = []\n        self.computer_card = []\n        self.user_points = 0\n        self.computer_points = 0\n        self.computer_hidden_point = 0\n        self.user_ace_adjusted = False\n        self.computer_ace_adjusted = False\n        self.heart_ranks = [\n            \"ace\",\n            \"two\",\n            \"three\",\n            \"four\",\n            \"five\",\n            \"six\",\n            \"seven\",\n            \"eight\",\n            \"nine\",\n            \"ten\",\n            \"jack\",\n            \"queen\",\n            \"king\",\n        ]\n        self.diamond_ranks = [\n            \"ace\",\n            \"two\",\n            \"three\",\n            \"four\",\n            \"five\",\n            \"six\",\n            \"seven\",\n            \"eight\",\n            \"nine\",\n            \"ten\",\n            \"jack\",\n            \"queen\",\n            \"king\",\n        ]\n        self.spade_ranks = [\n            \"ace\",\n            \"two\",\n            \"three\",\n            \"four\",\n            \"five\",\n            \"six\",\n            \"seven\",\n            \"eight\",\n            \"nine\",\n            \"ten\",\n            \"jack\",\n            \"queen\",\n            \"king\",\n        ]\n        self.club_ranks = [\n            \"ace\",\n            \"two\",\n            \"three\",\n            \"four\",\n            \"five\",\n            \"six\",\n            \"seven\",\n            \"eight\",\n            \"nine\",\n            \"ten\",\n            \"jack\",\n            \"queen\",\n            \"king\",\n        ]\n\n    def chip_counter(self) -> None:\n        bank = self.budget\n\n    def user_turn(self) -> tuple[Any, int, list, int, Literal['DEALER WINS!']] | tuple[Any, int]:\n        bust = self.user_points > 21\n\n        card_suit = random.choice(self.suits)\n\n        if card_suit == \"hearts\" and len(self.heart_ranks) > 0:\n            rank = random.choice(self.heart_ranks)\n            self.heart_ranks.remove(rank)\n\n        elif card_suit == \"diamonds\" and len(self.diamond_ranks) > 0:\n            rank = random.choice(self.diamond_ranks)\n            self.diamond_ranks.remove(rank)\n\n        elif card_suit == \"spades\" and len(self.spade_ranks) > 0:\n            rank = random.choice(self.spade_ranks)\n            self.spade_ranks.remove(rank)\n\n        elif card_suit == \"clubs\" and len(self.club_ranks) > 0:\n            rank = random.choice(self.club_ranks)\n            self.club_ranks.remove(rank)\n\n        self.user_cards.append((card_suit, rank))\n        user_cards = self.user_cards\n        new_card = user_cards[-1]\n        new_card_pts_key = user_cards[-1][1]\n\n        if new_card_pts_key in self.points_dictionary:\n            self.user_points += self.points_dictionary[new_card_pts_key]\n            #check that the user doesn't have an ace first. Note that this does permanently alter self.user_cards.\n            user_has_ace = any(rank == \"ace\" for suit, rank in self.user_cards[:-1])\n            if new_card_pts_key == \"ace\" and not user_has_ace:\n                if self.user_points < 21 and (self.user_points + 10) <= 21:\n                    self.user_points += 10\n                elif self.user_points < 21 and (self.user_points +10) > 21:\n                    self.user_ace_adjusted = True\n            elif self.user_points > 21 and user_has_ace and not self.user_ace_adjusted:\n                self.user_points -= 10\n                # Only adjust an ace from 11->1 once per user turn\n                self.user_ace_adjusted = True\n            elif self.user_points > 21:\n                bust = True\n                computer_points = self.computer_points + self.computer_hidden_point\n                return new_card, self.user_points, self.computer_card, computer_points, \"DEALER WINS!\"\n\n        return new_card, self.user_points\n    \n    #TODO refactor the below to use self.computer_points rather than the local variable computer_points.\n    def ai_turn(self) -> tuple[list, int, Literal['PUSH']] | tuple[list, int, Literal['DEALER WINS!']] | tuple[list, int, Literal['YOU WIN!']]:\n        \"\"\"This method is called after the user stands to execute the dealer's turn. \n        It implements the standard dealer rules of hitting until 17 or higher, \n        and then compares against the user's points to determine the outcome.\"\"\"\n        \n        self.computer_ace_adjusted = False\n        total = self._hand_total(self.computer_card)\n\n        # If dealer already meets stopping condition, return immediately\n        if total >= 17:\n            if total == self.user_points:\n                return self.computer_card, total, \"PUSH\"\n            if total <= 21 and total > self.user_points:\n                return self.computer_card, total, \"DEALER WINS!\"\n            if total > 21:\n                return self.computer_card, total, \"YOU WIN!\"\n            # Dealer stands but has less than player -> player wins\n            return self.computer_card, total, \"YOU WIN!\"\n\n        # Otherwise draw until stopping rule is reached\n        while True:\n            card_suit = random.choice(self.suits)\n\n            if card_suit == \"hearts\" and len(self.heart_ranks) > 0:\n                rank = random.choice(self.heart_ranks)\n                self.heart_ranks.remove(rank)\n            elif card_suit == \"diamonds\" and len(self.diamond_ranks) > 0:\n                rank = random.choice(self.diamond_ranks)\n                self.diamond_ranks.remove(rank)\n            elif card_suit == \"spades\" and len(self.spade_ranks) > 0:\n                rank = random.choice(self.spade_ranks)\n                self.spade_ranks.remove(rank)\n            elif card_suit == \"clubs\" and len(self.club_ranks) > 0:\n                rank = random.choice(self.club_ranks)\n                self.club_ranks.remove(rank)\n            else:\n                # no more cards to draw\n                break\n\n            self.computer_card.append((card_suit, rank))\n\n            # Recompute total and decide\n            total = self._hand_total(self.computer_card)\n\n            if total >= 17:\n                if total == self.user_points:\n                    return self.computer_card, total, \"PUSH\"\n                if total <= 21 and total > self.user_points:\n                    return self.computer_card, total, \"DEALER WINS!\"\n                if total > 21:\n                    return self.computer_card, total, \"YOU WIN!\"\n                # Dealer stands but has less than player -> player wins\n                return self.computer_card, total, \"YOU WIN!\"\n\n        # If we exit drawing loop without a decisive result, return current state\n        return self.computer_card, total, \"\"\n
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/backend/blackjack_engine.py b/backend/blackjack_engine.py
--- a/backend/blackjack_engine.py	(revision a22b2f132f56ca4c691e6e39ff24aa7f296a9583)
+++ b/backend/blackjack_engine.py	(date 1771246906948)
@@ -94,8 +94,6 @@
         """This method is called at the start of a new game to deal the initial two cards for the user and compute the initial points.
         It also checks for an initial blackjack and returns the appropriate result if so."""
 
-        card_suit = ""
-        rank = ""
 
         for card in range(2):
             while True:
@@ -161,7 +159,7 @@
 
         return self.user_cards, self.user_points, None, None
 
-    def _hand_total(self, cards: list[tuple]) -> int:
+    def hand_total(self, cards: list[tuple]) -> int:
         """Return best total for a hand treating aces as 1 or 11.
 
         Uses base values from `self.points_dictionary` (ace=1) and upgrades
@@ -185,7 +183,8 @@
 
         return total
 
-    def ai_new_game(self) -> tuple[list, int, Literal['BLACKJACK'], Literal['PUSH']] | tuple[list, int, Literal['BLACKJACK'], None]:
+    def ai_new_game(self) -> tuple[list[Any], int, str, str] | tuple[list[Any], int, str, None] | tuple[
+        list[Any], int, None, None]:
         
         """This method is called immediately after new_game_state() to set up the computer's hand and points based on the initial two cards. 
         It also checks for an initial blackjack and returns the appropriate result if so."""
@@ -221,7 +220,7 @@
             self.computer_card.append((card_suit, rank))
 
         # Use the robust hand-total helper to compute points
-        total = self._hand_total(self.computer_card)
+        total = self.hand_total(self.computer_card)
         self.computer_points = total
         # After computing total we don't need a separate hidden-point mechanic
         self.computer_hidden_point = 0
@@ -363,13 +362,14 @@
         return new_card, self.user_points
     
     #TODO refactor the below to use self.computer_points rather than the local variable computer_points.
-    def ai_turn(self) -> tuple[list, int, Literal['PUSH']] | tuple[list, int, Literal['DEALER WINS!']] | tuple[list, int, Literal['YOU WIN!']]:
+    def ai_turn(self) -> tuple[list[Any], int, str]:
         """This method is called after the user stands to execute the dealer's turn. 
         It implements the standard dealer rules of hitting until 17 or higher, 
         and then compares against the user's points to determine the outcome."""
         
         self.computer_ace_adjusted = False
-        total = self._hand_total(self.computer_card)
+
+        total = self.hand_total(self.computer_card)
 
         # If dealer already meets stopping condition, return immediately
         if total >= 17:
@@ -405,7 +405,7 @@
             self.computer_card.append((card_suit, rank))
 
             # Recompute total and decide
-            total = self._hand_total(self.computer_card)
+            total = self.hand_total(self.computer_card)
 
             if total >= 17:
                 if total == self.user_points:
